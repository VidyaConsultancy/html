/**
 * Addition +
 * Substract -
 * Multiply *
 * Divide /
 * Remainder %
 */

const a = 10;
const b = 20;

console.log(12 >= a && 12 <= b);
console.log(12 >= a || 12 <= b);
console.log(!(a > b));

let foo;

const bar = foo ?? "baz";
const baz = foo ? "bar" : "baz";
console.log(bar);
